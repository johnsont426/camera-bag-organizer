<h1>Your Camera Bag:</h1>
<% @user.bags.each_with_index do |bag, index| %>
	<h2><a href="/bags/<%= bag.id %>">Bag <%= index + 1 %></a>(<%= bag.bag_type %>)</h2>
	<h3>Weight: <%= bag_weight(bag) %>g</h3>
<% end %>
	

